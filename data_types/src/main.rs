fn main(){
    // enteros
	let numero_uno: i8 = -10;
	let numero_dos: u8 = 29;
	println!("{} {}", numero_uno, numero_dos);

    // char
    let name = "Jaz";
	let emoji = "🐳";
	println!("{} {}", name, emoji);

    // float
    let float = 12.5;
	let pi: f32 = 3.14;
	println!("{} {}", float, pi);

    // booleano
    let result: bool = false;
	println!("{}", result);
}

// Tambien existen los tipos de Datos str y String, pero hay secciones dedicadas a ello más adelante.